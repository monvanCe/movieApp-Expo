import { Text, View } from 'react-native';

export default function () {
  return (
    <View
      style={{
        padding: 10,
        backgroundColor: 'white',
        width: '100%',
      }}
    >
      <Text style={{ fontSize: 20, fontWeight: 'bold' }}>Lists</Text>
    </View>
  );
}
